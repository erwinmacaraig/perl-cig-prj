[% IF Navigation %]
<div class = "col-md-12">
	<h2 class="main-heading">[% PageTitle %]</h2>
</div>
<div class = "col-md-12 steps">
	[% Navigation %]
	[% PageInfo %]
</div>
[% END %]

[% Title %]

[% IF Errors.size %]
<div class="col-md-12 error-alerts">
	<div class="alert">
		<div>
			<span class="fa fa-exclamation"></span>
			[% IF Errors.size > 1%]
				<p>An error occured</p>
				<ul>
				[% FOREACH error IN Errors %]
					<li>[% error %]</li>
				[% END %]
				</ul>
				<p></p>
				<p></p>
			[% ELSIF Errors.size == 1 %]
				[% FOREACH error IN Errors %]
					<p>[% error %]</p>
				[% END %]
			[% END %]
		</div>
	</div>
</div>
[% END %]

<div class="col-md-12">
	[% TextTop %]
	<div class = "notavailable" style="display:none;">
	    <div class="alert">
	        <div>
	            <span class="fa fa-exclamation"></span>
	            <p>[% Lang.txt("This type of registration is not available") %]</p>
	        </div>
	    </div>
	</div>
</div>

<div class="[% IF processStatus == 1 %]col-md-12[% ELSE %]col-md-9[% END %]">
[% DocUploader %]
	<form action = "[% IF !altContinue %][% Target %][% ELSE %][% altContinueFormConfig.Target %][% END %]" method = "POST" id = "[% FormID or 'flowFormID' %]">
	    [% IF !altContinue and FlowNextAction %] <input type = "hidden" name ="rfp" value ="[% FlowNextAction %]"> [% END %]
        [% IF !altContinue %][% HiddenFields %][% ELSE %][% altContinueFormConfig.HiddenFields %][% END %]
	    [% IF Content %][% Content %][% END %]

		<div class = "button-row">
			<div class="txtright">
			[% IF !NoBackButton and BackButtonURL %]
				<a href = "[% BackButtonURL %]" id = "flow-btn-back" class = "[% IF processStatus == 1 %]nudge[% END %]">[% IF BackButtonText %][% Lang.txt(BackButtonText) %][% ELSE %][% Lang.txt('Back') %][% END %]</a>
			[% END %]
			[% IF !NoContinueButton %]
				<input id = "flow-btn-continue" type = "submit" value = " [% Lang.txt(ContinueButtonText) %] "  class = "btn-main btn-proceed">
			[% END %]
			[% IF altContinue and NoContinueButton %]
				<input id = "flow-btn-continue" type = "submit" value = " [% Lang.txt(altContinueButtonText) %] "  class = "btn-main btn-proceed">
			[% END %]
			</div>
		</div>
    	</form>
</div>

[% IF FlowSummary or FlowSummaryContent %]
<div class="col-md-3">
    [% IF FlowSummaryContent %]
        [% FlowSummaryContent %]
    [% ELSE %]
        [% FStemplate = FlowSummaryTemplate or 'flow/flow_summary.templ' %]
        [% INCLUDE $FStemplate %]
    [% END %]
</div>
[% END %]
<br/>

[% TextBottom %]
